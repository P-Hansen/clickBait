/// @description Insert description here

draw_self();

/*
if(instance_exists(fish)){
	
	offsetX = lengthdir_x(80, image_angle);
	offsetY = lengthdir_y(80, image_angle);
	
	draw_line_width(x + offsetX, y-82 + offsetY, fish.x, fish.y, 3);
	
} else if (image_angle > 0){
	
	offsetX = lengthdir_x(60, image_angle);
	offsetY = lengthdir_y(60, image_angle);
	
	draw_line_width(x + offsetX, y-82 + offsetY, x+160, y+50, 3);
	
} else {
	draw_line_width(x+80, y-82, x+160, y+50, 3);
}
*/